---
import Services from "./Services.astro";

const employeesData = await fetch(
  "https://frontend-design-theme.netlify.app/api/employees"
).then((response) => response.json());

console.log(employeesData);
---

<section class="hero">
  <div class="hero-text">
    <h1>Empowering Your Business Growth</h1>
    <p>
      Discover tailored strategies and expert insights to take your business to
      the next level. Let us help you navigate challenges and achieve
      sustainable success.
    </p>
  </div>
  <div class="image">
    <img src="../../public/images/hero-img.jpg" alt="Kvinde i arbejde" />
  </div>
  <Services />
</section>

<style>
  @layer components {
    section {
      display: grid;
      grid-template-columns: 1fr 1fr;
      grid-column: 1 / -1;
    }

    div {
      display: grid;
    }
    .hero-text {
      grid-column: 1;
      background-color: black;
      width: 100%;
      height: 100vh;
    }

    .image {
      grid-column: 2;
      height: 100vh;
    }

    .image img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
  }
</style>
