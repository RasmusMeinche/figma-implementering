---

---

<section>
  <div>
    <p>Case Studies</p>
    <h1>Taxes & Efficiency</h1>
  </div>
</section>

<style>
  section {
    background-image: url("../images/studyhero.png");
    display: grid;
    grid-template-columns: subgrid;
    grid-column: 1 / -1;
    align-items: center;
    background-size: cover;
    background-repeat: no-repeat;
    width: 100%;
    height: 50svh;

    div {
      display: flex;
      flex-direction: column;
      grid-column: content;
      z-index: 2;
      background-color: rgba(255, 255, 255, 0.85);
      padding: 2rem;
      place-self: center start;
      border-radius: 1rem;
      width: fit-content;
      inline-size: max-content;
    }

    p {
      background-color: var(--button-color);
      padding: var(--padding-kicker);
      border-radius: var(--border-rad);
      margin-bottom: 1rem;
      width: fit-content;
    }

    h1 {
      font-size: var(--step-2);
      font-weight: bold;
      max-width: 25ch;
    }
  }
</style>
